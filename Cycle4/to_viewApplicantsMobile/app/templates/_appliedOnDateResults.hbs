<a href="#" class="pad--sm--m">
    <h4 class="card__heading dots">{{application.First_Name__c}} {{application.Last_Name__c}}</h4>
    <small class="card__subheading dots">Applied On: 
        {{formatDate application.Applied_On__c}}
    </small>
</a>
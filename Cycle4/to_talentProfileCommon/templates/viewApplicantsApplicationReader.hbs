<div class="content pad--sm--n">
	{{partial "applicationView"}}
    <div class="hide">
		{{provide-feedback ctrl=this buttonClass="js-feedback-button"}}

        {{#unless hasOutcome}}
		  {{update-status ctrl=controller buttonClass="js-status-button"}}
        {{/unless}}
	</div>
</div>